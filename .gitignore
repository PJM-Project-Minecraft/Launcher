node_modules/
dist/

backend/data/
backend/tmp/
backend/storage/

# Скомпилированные бинари бэкенда (собираются в Docker)
backend/server
backend/bot

# Сборочные артефакты
anticheat-native/build/
dashboard/tsconfig.tsbuildinfo

# Локальная dev-БД и стораджи
data/

src-tauri/target/
launcher-slint/target/

dashboard/node_modules/
dashboard/.next/
dashboard/out/

.env
backend/.env
dashboard/.env.local

# Приватный ключ подписи автообновления (updatesign keygen) — ТОЛЬКО на релиз-боксе,
# в git попасть не должен: с ним можно подписать вредоносное обновление.
*.key
update-signing.key

# Прод-only оверрайд compose (живёт только на VPS, деплой его не трогает)
docker-compose.override.yml
.superpowers/

# Сторонний authlib-injector пользователя (отдельный репозиторий, не часть монорепо)
/Gml.Authlib.Injector/
.aider*

# NeoForge-мод: сборочные артефакты
anticheat-neoforge/build/
anticheat-neoforge/.gradle/
